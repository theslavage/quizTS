# 🧠 QuizTS — SPA-квиз на TypeScript

## 🔧 Предварительные требования

Перед запуском убедитесь, что у вас установлено:

- **Node.js** (LTS 18.x / 20.x)
- **npm**
- **TypeScript**
- **локальный сервер** (например, `lite-server`, `webpack-dev-server` или любой другой, который используется в проекте)

Если TypeScript не установлен глобально:
- npm install -g typescript

📦 Установка проекта
1. Клонировать репозиторий
- git clone https://github.com/theslavage/quizTS.git
 - cd quizTS

2. Установить зависимости
- npm install

🚀 Запуск проекта
🟢 Режим разработки

Обычно проект запускается одной из команд (оставь свою реальную из package.json):
- npm run dev
или
- npm start
